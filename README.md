App de Controle de Caixa feito em Python, usando pandas para a manipulação de csv e amazenamento de dados, e tkinter para a interface gráfica. 

pip install pandas
pip install tkinter
pip install openpyxl
